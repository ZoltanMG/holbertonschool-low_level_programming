#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int letra = 97;
while (letra < 124)
{
putchar(letra);
letra = letra + 1;
if x(letra == 123)
{
putchar('\n');
}
}
return (0);
}
